"let
    Source = MyFunction(2, 4)
in
    Source

let
    Source = tbl1,
    STEP1 = Table.ToRecords(Source),
    STEP2 = each Record.AddField(Source{_}, Record.FieldNames(Source{_}){_}& "AddThisText", "VALUE")


in
    STEP2
